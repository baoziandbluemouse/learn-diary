#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    cin >> s;
    map<string, int> mp;
    int n = s.size();
    s = " " + s;
    for (int len = 1; len <= n; len++)
    {
        for (int i = 1; i <= n; i++)
        {
            for (int j = i)
        }
    }
}